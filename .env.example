# Environment
ENVIRONMENT=development

# Redis
REDIS_URL=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT (development only - CHANGE IN PRODUCTION)
JWT_SECRET_KEY=dev-secret-key-change-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# SERVER
GRPC_PORT=50051
HTTP_HEALTH_PORT=8081

# Base de datos
DB_HOST=localhost #postgres
DB_PORT=5432
DB_NAME=osmidb
DB_USER=osmi
DB_PASSWORD=your_database_password
DB_SSLMODE=disable

# PostgreSQL (Docker Compose)
DATABASE_URL=postgresql://osmi:osmi1405@localhost:5432/osmidb

# Database connection pool
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=1h
DB_CONN_MAX_IDLE_TIME=30m

# Servidor gRPC
GRPC_PORT=50051
GRPC_HOST=0.0.0.0

# Gateway HTTP
HTTP_PORT=8080
HTTP_HOST=0.0.0.0

# CORS
CORS_ORIGINS=http://localhost:3000

# Servidor gRPC
GRPC_PORT=50051
GRPC_HOST=0.0.0.0

# Gateway HTTP
HTTP_PORT=8080
HTTP_HOST=0.0.0.0

# CORS
CORS_ORIGINS=http://localhost:3000
